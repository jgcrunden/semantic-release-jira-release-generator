name: CI

on:
  push:
    branches-ignore: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run unit tests
        uses: actions/setup-node@latest
        with:
          node-version: 20.x
      - run: npm ci
      - run: npm run lint
      - run: npm run test