{
  "name": "semantic-release-jira-release-generator",
  "version": "1.0.0",
  "description": "Creates a Release in Jira and associates any Jira issues referenced in commit messages to that release",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --silent --coverage"
  },
  "author": "Joshua Crunden",
  "license": "GPL-3.0-only",
  "dependencies": {
    "@semantic-release/error": "^4.0.0",
    "axios": "^1.7.2"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
